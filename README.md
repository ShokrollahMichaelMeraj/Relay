# Relay
Relay is a dependency-aware execution system for AI work: you define a goal as a set of tasks, connect tasks with dependencies (arrows), choose which model/agent runs each task, then Relay executes the plan (parallel + sequential), records everything, and lets you inspect/approve outputs.
